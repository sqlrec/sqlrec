apiVersion: v1
kind: PersistentVolume
metadata:
  name: ${RESOURCE_PV_NAME}
spec:
  storageClassName: ""
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: ${LIB_DIR}
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${RESOURCE_PVC_NAME}
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  volumeName: ${RESOURCE_PV_NAME}
